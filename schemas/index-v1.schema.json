{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/task-v1/luminesk-cli/2.0/schemas/index-v1.schema.json",
  "title": "Luminesk database index v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["indexVersion", "revision", "entries"],
  "properties": {
    "indexVersion": {"const": 1},
    "revision": {
      "type": "string",
      "pattern": "^[0-9a-f]{40}$"
    },
    "entries": {
      "type": "array",
      "maxItems": 10000,
      "items": {"$ref": "#/$defs/entry"}
    }
  },
  "$defs": {
    "digest": {
      "type": "string",
      "pattern": "^sha256:[0-9a-f]{64}$"
    },
    "identifier": {
      "type": "string",
      "pattern": "^[a-z0-9](?:[a-z0-9._-]{0,126}[a-z0-9])?$"
    },
    "semver": {
      "type": "string",
      "pattern": "^(?:0|[1-9][0-9]*)\\.(?:0|[1-9][0-9]*)\\.(?:0|[1-9][0-9]*)(?:-[0-9A-Za-z-]+(?:\\.[0-9A-Za-z-]+)*)?(?:\\+[0-9A-Za-z-]+(?:\\.[0-9A-Za-z-]+)*)?$"
    },
    "entry": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "displayName",
        "recipeVersion",
        "kind",
        "game",
        "edition",
        "summary",
        "keywords",
        "path",
        "manifestDigest"
      ],
      "properties": {
        "name": {"$ref": "#/$defs/identifier"},
        "displayName": {"type": "string", "minLength": 1},
        "recipeVersion": {"$ref": "#/$defs/semver"},
        "kind": {"enum": ["core", "template"]},
        "game": {"const": "minecraft"},
        "edition": {"enum": ["java", "bedrock", "cross-platform"]},
        "summary": {"type": "string", "minLength": 1},
        "keywords": {
          "type": "array",
          "items": {"type": "string", "minLength": 1}
        },
        "path": {"$ref": "#/$defs/identifier"},
        "manifestDigest": {"$ref": "#/$defs/digest"},
        "templateDigest": {"$ref": "#/$defs/digest"}
      }
    }
  },
  "$comment": "Entries must be sorted by name, normalized names must be unique, and each path must equal its name; the CLI enforces these semantic constraints."
}
